<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!--
      Splash theme (P11). Generated by react-native-bootsplash, with one line
      added by hand: bootSplashBrand.

      Android 12+ draws the splash itself and masks bootSplashLogo into a
      circle, so the logo here is the heart alone (the adaptive-icon foreground,
      already sized for that mask). The wordmark would be clipped away, so it
      goes to the branding image the system draws at the bottom instead.

      The generator's brand flag is a paid feature, but the underlying attribute
      is not: bootSplashBrand maps straight to
      android:windowSplashScreenBrandingImage in the library's values-v31 style,
      so pointing it at a drawable we resized ourselves costs nothing.
      Consequence to remember: bootsplash generate will rewrite this file and
      drop the line, so re-add it after any regeneration.

      Android 11 and below take the compat path, which centres the logo and has
      nowhere to put a branding image; those show the heart only.
    -->
    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="bootSplashBrand">@drawable/bootsplash_brand</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>

</resources>
